# CICD workflow for DevOps Evolution 2026 project
name: DevOps Evolution 2026 Pipeline


# 2. THE TRIGGER: When should this automation execute?
on:
  push:
    branches:
      - main
      - hardcode

# 3. THE WORK: What actions should the remote machine take?

jobs:
  build-and-verify:

      # Tell GitHub to spin up a fresh, free Ubuntu Linux cloud container for you
    runs-on: ubuntu-latest

    steps:
      # Step A: Clone your GitHub repository code down onto the remote container

     - name: checkout code
       uses: actions/checkout@v4

        # Step B: Run a simple echo command to verify execution
     - name: Execute Shell testing greeting
       run: |
          echo "=========================================================="
          echo "HELLO NAVYAKANTH! YOUR AUTOMATION WORKS LIVE ON THE CLOUD"
          echo "=======================================================

      # Step C: Set up Terraform on the remote container   
